name: Sækja fréttir

on:
  schedule:
    - cron: '*/5 * * * *' # Breytt í 5 mínútur
  workflow_dispatch:      # Leyfa handvirka keyrslu

jobs:
  cron-job:
    runs-on: ubuntu-latest
    steps:
      - name: Sækja fréttir
        # Mundu að setja þína slóð hér!
        run: curl -L https://news-mvp-five.vercel.app/api/ingest
